# Railway Environment Variables Template
# Copy this to Railway's environment variables section

# ===== BACKEND SERVICE =====

# Google OAuth2 Configuration (REQUIRED)
GOOGLE_CLIENT_ID=your_client_id_from_google_cloud_console
GOOGLE_CLIENT_SECRET=your_client_secret_from_google_cloud_console

# Session Secret (REQUIRED - Generate with: openssl rand -base64 32)
SESSION_SECRET=CHANGE_THIS_TO_RANDOM_32_CHAR_STRING

# Frontend URL (Update after deploying frontend)
FRONTEND_URL=https://your-frontend-production.up.railway.app

# OAuth Redirect URI (Update with your backend URL)
GOOGLE_REDIRECT_URI=https://your-backend-production.up.railway.app/api/auth/google/callback

# Google API Scopes
GOOGLE_SCOPES=https://www.googleapis.com/auth/drive https://www.googleapis.com/auth/spreadsheets

# Environment
NODE_ENV=production

# Optional: Enable server authentication (no login required)
# First run locally: cd backend && npm run get-tokens
# GOOGLE_REFRESH_TOKEN=paste_refresh_token_here

# ===== FRONTEND SERVICE =====

# Backend API URL (Update after deploying backend)
REACT_APP_API_URL=https://your-backend-production.up.railway.app

# Environment
NODE_ENV=production